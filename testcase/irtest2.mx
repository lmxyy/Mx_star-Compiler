int main()
{
    int a = 1,b = 2;
    // int c = a == 1?a:b;
    // int c = 0;
    // if (a != 1)
    // 	c = a;
    // else c = b;
    // return c;
    bool b1 = a < b,b2 = a > b,b3 = a == b;
    if (b1||b2||b3) {
	b = 4;
    }
    return 0;
}
